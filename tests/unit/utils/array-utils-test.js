import arrayUtils from '../../../utils/array-utils';
import { module, test } from 'qunit';

module('Unit | Utility | array utils');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = arrayUtils();
  assert.ok(result);
});
