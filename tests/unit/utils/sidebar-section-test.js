import sidebarSection from '../../../utils/sidebar-section';
import { module, test } from 'qunit';

module('Unit | Utility | sidebar section');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = sidebarSection();
  assert.ok(result);
});
