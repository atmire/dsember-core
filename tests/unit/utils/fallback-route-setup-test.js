import fallbackRouteSetup from '../../../utils/fallback-route-setup';
import { module, test } from 'qunit';

module('Unit | Utility | fallback route setup');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fallbackRouteSetup();
  assert.ok(result);
});
