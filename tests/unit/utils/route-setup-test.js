import routeSetup from '../../../utils/route-setup';
import { module, test } from 'qunit';

module('Unit | Utility | route setup');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = routeSetup();
  assert.ok(result);
});
