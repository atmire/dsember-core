import { formattedCurrentDate } from '../../../helpers/formatted-current-date';
import { module, test } from 'qunit';

module('Unit | Helper | formatted current date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formattedCurrentDate(42);
  assert.ok(result);
});
